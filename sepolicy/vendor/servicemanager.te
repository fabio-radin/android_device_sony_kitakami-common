allow servicemanager hal_power_default:dir rw_dir_perms;
allow servicemanager hal_power_default:file rw_file_perms;
allow servicemanager hal_power_default:process getattr;
allow servicemanager init:binder transfer;
allow servicemanager init:dir search;
allow servicemanager init:file { open read r_file_perms };
allow servicemanager init:process getattr;
allow servicemanager mediaswcodec:dir search;
allow servicemanager mediaswcodec:file { open read };
allow servicemanager mediaswcodec:process getattr;
allow servicemanager per_proxy:dir search;
allow servicemanager per_proxy:file { open read r_file_perms };
allow servicemanager per_proxy:process getattr;

